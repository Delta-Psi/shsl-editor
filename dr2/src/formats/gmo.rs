/*
#[derive(Debug)]
pub enum Error {
    Io(std::io::Error),
}

pub struct Gmo<'a> {
}

impl<'a> Gmo<'a> {
    pub fn from_bytes(data: &'a [u8]) -> Result<Error, Self> {
        panic!()
    }
}

pub struct Chunk<'a> {
    pub type_: u16,
    pub header: &'a [u8],
}
*/
